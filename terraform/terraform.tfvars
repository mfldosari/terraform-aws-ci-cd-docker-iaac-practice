key_name        = "key"                     # This should be the *name* of the key pair created in AWS (not the path)
instance_type   = "t2.micro"                       # Free-tier eligible and good for small apps
ami_id          = "ami-0c7217cdde317cfec"          # Ubuntu Server 22.04 LTS in us-east-1
